language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7-dev"

matrix:
  allow_failures:
    - python: 3.7-dev

cache: pip

install:
  - pip install -r requirements.txt
  - pip install -r test-requirements.txt
  - pip install flake8
script:
  - flake8 rfdevices/
  - nosetests tests/
